# CLAUDE.md — 데이터 추가·배포 자동화 규칙

자녀용 영어 학습 웹앱(정적 사이트, GitHub Pages). **단어(Vocabulary·DAY)** 와 **독해(Reading·UNIT)** 두 도메인.
사진 판독은 앱이 아니라 이 대화에서 한다. 아래는 **이미지 첨부 → 데이터 생성 → 배포**를 한 번에 처리하는 규칙이다.

## 트리거
사용자가 **이미지를 첨부**하고 다음처럼 말하면 아래 전체 절차를 끝까지 수행한다.

- `dayNN 만들어줘` · "단어 …" → **단어(Vocabulary)** 파이프라인
- `unitNN 만들어줘` · "독해 …" → **독해(Reading)** 파이프라인

규칙:
- 트리거의 번호(`NN`)는 **자리표시**일 뿐이다. **실제 번호·제목은 이미지 머리글에서 읽는다**
  (예: `DAY 11  Jobs & Work`, `UNIT 07  Arts`). 기존 **파일명 포맷을 그대로 유지**한다: `dayNN.json`, `unitNN.json`.
- day·unit은 **따로 또는 함께** 요청될 수 있다. 두 판형은 시각적으로 구분된다
  (**단어** = DAY 머리글·`어휘/뜻` 2열 / **독해** = UNIT 머리글·`구분 · # · 어휘 · 뜻` 4열).
  첨부 이미지가 어느 쪽인지 스스로 판별해 알맞은 파이프라인에 배정한다.
- 함께 요청됐는데 이미지가 섞여 어느 쪽인지 **애매하면 진행하지 말고**,
  "day와 unit을 각각 따로 요청해 달라"고 사용자에게 말한다.

## 판독 규칙
- 단어: `docs/prompt-vocabulary.md` 의 `---` 사이 규칙을 **그대로** 적용.
- 독해: `docs/prompt-reading.md` 의 `---` 사이 규칙을 **그대로** 적용.
- 첨부 이미지를 **직접 판독**한다(별도 모바일 대화 불필요).
- 먼저 **검증표**(`# · 영어 · 뜻`)를 보여준다. 흐릿해 확신이 안 서는 항목은 `⚠️` 로 표시해
  **되묻는다(추측 금지)**.

## 시작 전 동기화 (필수)
**어떤 작업이든 손대기 전에 먼저 원격 최신 소스를 받아 동기화한다.** 다른 기기·세션에서 올린
커밋이 있을 수 있으므로, 낡은 소스 위에서 작업하거나 push 단계에서 거절당하는 일을 막는다.

```
git fetch origin main && git status -sb     # 뒤처졌는지 먼저 확인
git pull --rebase origin main               # 뒤처졌으면 최신 위로 얹는다
```

- 판독·JSON 생성·코드 수정 **어느 것이든 이 단계를 먼저** 거친다.
- 그래도 push 가 거절되면(작업 중 새 커밋이 올라온 경우) `git rebase origin/main` 후 다시 push 한다.
- 충돌이 나면 임의로 덮어쓰지 말고 사용자에게 알린다. `--force` 계열은 쓰지 않는다.

## 생성 절차
1. 판독 후 **검증표**를 제시한다.
2. 리포 루트에 입력 JSON을 저장한다(둘 다 `.gitignore` 대상 — 커밋되지 않는다):
   - 단어 → `words.json` : `{"groups":[{"title":"DAY NN  …","words":[["en","뜻"], …]}]}`
   - 독해 → `reading.json` : `{"unit":"UNIT NN  …","passages":[{"title":"…","words":[["en","뜻"], …]}]}`
3. 변환 스크립트를 실행한다:
   - `python scripts/import_words.py words.json`   → `data/vocabulary/dayNN.json` + `index.json` 재생성
   - `python scripts/import_reading.py reading.json` → `data/reading/unitNN.json` + `index.json` 재생성
4. `git diff` 로 생성물을 확인한다.

## 배포
0. push 전에 **원격이 앞서 있지 않은지 다시 확인**한다(위 「시작 전 동기화」와 동일한 방법).
1. **먼저 `main` 에 직접 commit·push 를 시도**한다.
2. 직접 push 가 **차단·권한없음으로 실패하면**, 지정 개발 브랜치에 push 하고 **PR 을 생성**한 뒤
   사용자에게 알린다(모바일에서 1탭 merge → Pages 자동 재배포).
3. 모드(모바일/비모바일) 감지는 **하지 않는다**. 직접 배포를 시도한 **결과로 자동 분기**한다
   — 직접 배포 가능 = `main` 즉시 반영, 불가 = PR 경유.

## 원칙 (지킬 것)
- 백엔드·API 키·빌드 단계 없음(정적·무료), 외부 라이브러리 없음.
- `data/**/*.json` 의 `ko`(뜻)는 **원자료 그대로 보존** — 품사 박스·개행 같은 표기 변환은 표시 단계에서만.
- **한 UNIT = JSON 한 개**(사진 여러 장이 한 UNIT 을 이룰 수 있음).
- 앱 데이터 fetch 에 캐시 무효화(`?v=시각`)가 있어 반영 즉시 보인다.
